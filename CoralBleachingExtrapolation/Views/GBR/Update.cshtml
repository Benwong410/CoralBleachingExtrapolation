@model GBRCoralPoint
@*
<div class="container mt-4">
    <div class="row">
        <class ="card">
            <div class="card-header bg-primary text-white">
                <h2> Update Global Coral</h2>
            </div>
            <div ="card-body">
                <form asp-controller="GBR" asp-action="Update" method="post">
                    <input type="hidden" asp-for="GBRCoralPointID" />
                    <div class="form-group mb-3">
                        <label asp-for="ReefName" class="form-label">Reef Name</label>
                        <input asp-for="ReefName" class="form-control" placeholder="Enter the Reef Name"></input>
                    </div>
                    <div class="form-group mb-3">
                        <label asp-for="Point" class="form-label">Point</label>
                        <input asp-for="Point" class="form-control" placeholder="Enter the Point"></input>
                    </div>
                    <div class="form-group mb-3">
                        <label asp-for="Latitude" class="form-label">Latitude</label>
                        <input asp-for="Latitude" class="form-control" placeholder="Enter the Latitude"></input>
                    </div>
                    <div class="form-group mb-3">
                        <label asp-for="Longitude" class="form-label">Longitude</label>
                        <input asp-for="Longitude" class="form-control" placeholder="Enter the Longitude"></input>
                    </div>
                    <div class="form-group mb-3">
                        <label asp-for="ReportYear" class="form-label">Report Year</label>
                        <input asp-for="ReportYear" class="form-control" placeholder="Enter the Report Year"></input>
                    </div>
                    <input type="hidden" name="ShapeWkt" value="@Model.Point?.ToString()" />
                    <div type="hidden"  class="form-group mb-3">
                        <label asp-for="GBRCoralPropertiesFK" class="form-label">Species</label>
                        <input asp-for="GBRCoralPropertiesFK" class="form-control" placeholder="asdasd"></input>
                    </div>
                    
                    <div class="d-flex justify-content-between">
                        <button type="submit" class="btn btn-primary">Update</button>
                        <a asp-controller="Home" asp-action="Index" class="btn btn-danger">Cancel</a>
                    </div>

                 


                    <!-- missing shape: needa figure out how we accept an coodinate and create a polygon -->
                    <!-- missing area: we could cal the area based on the polygon -->
                    <!-- missing region: a drop box is needed to let client choose region -->
                </form>
            </div>
        </class>
    </div>
</div>
*@